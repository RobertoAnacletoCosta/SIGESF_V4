@model IEnumerable<SIGESF.Models.NaturezaDespesa>

@{
    ViewBag.Title = "Index";
}

<h2>RELAÇÃO DE NATUREZA DE DESPESA</h2>

<p>
    @Html.ActionLink("Adicionar Natureza de Despeza", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CodigoDespesa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CodigoDespesa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.ActionLink("Atualizar", "Edit", new { id=item.NaturezaDespesaId }) |
            @Html.ActionLink("Detalhar", "Details", new { id=item.NaturezaDespesaId }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.NaturezaDespesaId })
        </td>
    </tr>
}

</table>
